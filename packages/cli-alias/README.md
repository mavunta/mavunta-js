# @mavunta/cli

Alias of the canonical [`mavunta-cli`](https://www.npmjs.com/package/mavunta-cli) package (bin: `mavunta`). Both names install the same official Mavunta Pay CLI:

```bash
npm install -g mavunta-cli
```

Docs: https://developers.mavunta.com/docs
