# @mavunta/sdk

Alias of the canonical [`mavunta`](https://www.npmjs.com/package/mavunta) package. Both names install the same official Mavunta Pay SDK:

```bash
npm install mavunta
```

Docs: https://developers.mavunta.com/docs
