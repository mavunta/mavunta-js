/**
 * coinwaka — the official Coinwaka SDK bridge.
 *
 * This package re-exports `@coinwaka/sdk` so that `npm install coinwaka` works.
 * For new projects, prefer installing `@coinwaka/sdk` directly.
 */
export * from '@coinwaka/sdk'
export { Coinwaka as default } from '@coinwaka/sdk'
